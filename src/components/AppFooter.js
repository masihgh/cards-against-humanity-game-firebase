import React from 'react';
import {Card, CardBody, Typography} from "@material-tailwind/react";
import config from "@/config";

const AppFooter = () => {
    return (
        <footer className={'bg-white mt-5 shadow p-3'}>
            &copy HMM
        </footer>
    );
};

export default AppFooter;